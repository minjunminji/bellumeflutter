import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF0A6571);      // Dark teal primary
  static const background = Color(0xFFF5FCFF);   // Light blue background
  static const cardBackground = Color(0xFFFFFFFF); // White cards
  static const textPrimary = Color(0xFF333333);   // Dark gray text
  static const textSecondary = Color(0xFF666666); // Medium gray text
  static const textLight = Color(0xFF999999);     // Light gray text
  static const border = Color(0xFFE0E0E0);        // Light border
  static const error = Color(0xFFF44336);         // Red for errors
  static const success = Color(0xFF4CAF50);       // Green for success
  static const warning = Color(0xFFFF9800);       // Orange for warnings
  static const excellent = Color(0xFF4CAF50);     // Green
  static const good = Color(0xFF8BC34A);          // Light green  
  static const average = Color(0xFFFF9800);       // Orange
  static const needsImprovement = Color(0xFFF44336); // Red
} 